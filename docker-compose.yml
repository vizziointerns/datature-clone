services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: datature
      POSTGRES_PASSWORD: datature
      POSTGRES_DB: datature
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
